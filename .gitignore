# Backups locais (não enviar para o remoto — usar git revert/checkout)
_backups/

# Previews HTML de design (não fazem parte da app de produção)
_previews/
preview-*.html

# Configurações locais do Claude Code (per-machine, não compartilhar)
.claude/

# Sistema operacional
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# IDE / Editor
.vscode/*
!.vscode/launch.json
!.vscode/settings.json
!.vscode/tasks.json
.idea/
*.swp
*.swo
*~

# Logs e temporários
*.log
*.tmp
*.bak
*.old

# Dependências (caso futuramente seja adicionado Node.js)
node_modules/
package-lock.json
.npm/

# Arquivos sensíveis
.env
.env.local
secrets.json
credentials.json
js/data/api-key.js

# Scripts Vitória — contêm o TOKEN do MCP Sales Cube em texto plano (NUNCA commitar)
Faturamento-Vitoria.ps1
Leads-Vitoria.ps1
Leads-Vitoria-Campanha.ps1
Meta-Vitoria.ps1
Movimentacao-Leads.ps1
Negociacoes-Vitoria.ps1
Confirmacao-WS.ps1
Buscar-Treinamento.ps1
Mover-Etapa-Vitoria.ps1
Carteira-Leads-Vitoria.ps1
Buscar-Clientes-Vitoria.ps1
Lancar-Cliente.ps1
sc-web-auth.json
meta-master/Gerar-Dados-MetaMaster.ps1
Painel-Vitoria.ps1
Painel-Turma.ps1
Turma-Aluno.ps1

# Meta Master — dados de negócio + credenciais (NUNCA publicar)
meta-master/dados.js
meta-master/.auth
meta-master/fotos/
meta-master/mm-fotos.bak.js
meta-master/estado.json
meta-master/_tmp/

# Dados de negócio exportados (receita/faturamento/metas — não publicar)
RECEITA_*.csv
Faturamento_Vitoria_*.csv
Leads_Vitoria_*.csv
metas-vitoria.json
metas-leads-vitoria.json

# Previews de design (não fazem parte da app de produção)
*-preview.html
botao-copiar-card-consultor-10-opcoes.html
relatorio-filtros-status-simulacao.html

# Scripts que carregam o token do CRM em texto claro (auditoria 18/09/2026).
# Ficam fora do git para que um "git add ." nao amplie a exposicao do token.
Extraclasse-Pablo.ps1
Ponte-ZS.ps1

# Backups locais gerados pelo proprio app (copias de .ps1/.html com credenciais dentro)
meta-master/_bkp/
meta-master/_backups/
meta-master/_backup/

# Pitch: mora em disco e e publicado na Vercel, nunca pelo git.
# Alem da regra do projeto, a pasta tem binarios de 200-255 MB (video e .zip)
# que o GitHub recusa (limite de 100 MB/arquivo) e que incham o repositorio.
pitch/

# Testes: os scripts e o checklist sao versionados; os goldens e o retrato NAO.
# Eles sao gerados na maquina (regenera com -GravarGolden), refletem os dados
# reais desta instalacao e chegam a conter nome de pessoa nas chaves.
_tests/golden/
_tests/retrato-dados.json
